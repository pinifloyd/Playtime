.depot-form
  - form_tag do
    %fieldset
      %legend Please Log In
      %div
        %label= "Name:"
        = text_field_tag :name, params[:name]
      %div
        %label= "Password:"
        = password_field_tag :password, params[:password]
      %div
        = submit_tag "Login"